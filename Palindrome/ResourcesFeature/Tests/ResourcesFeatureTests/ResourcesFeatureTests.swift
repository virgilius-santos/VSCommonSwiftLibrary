import XCTest
@testable import ResourcesFeature

final class ResourcesFeatureTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ResourcesFeature().text, "Hello, World!")
//      Bundle.module
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
