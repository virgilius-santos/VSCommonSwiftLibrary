import XCTest
@testable import MQTTLibraryLive

final class MQTTLibraryLiveTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MQTTLibraryLive().text, "Hello, World!")
      XCUIElementSnapshot(
    }

    static var allTests = [
        ("testExample", testExample),
      
    ]
}
